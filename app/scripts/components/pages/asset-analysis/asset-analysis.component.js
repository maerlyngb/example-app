(function () {
    'use strict';

    // Register `assetAnalysis` component, along with its associated controller and template
    angular.
    module('assetAnalysis').
    component('assetAnalysis', {
        templateUrl: 'views/pages/asset-analysis.template.html',
        controller: [
            function assetAnalysisController() {
            }
        ]
    });
})();
